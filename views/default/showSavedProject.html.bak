{{from PIL import Image}}
{{extend 'layout.html'}}
<h1>This is the {{=project.width}} x {{=project.height}} project merged to one image:</h1>

{{index = 0}}
{{new_im = Image.new('RGB', (400,400))}}
{{for i in xrange(0,project.width*100+1,100):}}
    {{for j in xrange(0,project.height*100+1,100):}}
        {{im=Image.open(request.folder + 'uploads/' + images[0].file)}}
        {{im.thumbnail((100,100))}}
        {{new_im.paste(im, (j,i))}}
        {{index = index + 1}}
    {{pass}}
{{pass}}
